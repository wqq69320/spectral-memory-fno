# Model identifier.
name: mlp

# Input and output channel counts per grid point.
in_channels: 1
out_channels: 1

# Hidden width for fully connected layers.
hidden_dim: 128

# Number of hidden layers.
num_layers: 3

# Dropout probability used during training.
dropout: 0.0

# For Heat1D experiments, input_steps, pred_steps, and grid_size are inferred
# from the experiment and data configs.
